---
- hosts: PROD
  remote_user: root
  vars:
    root_app_dir: "/data/apache/serve/echaloasuerte/legacy/app/"
    root_log_dir: "/data/apache/serve/echaloasuerte/legacy/log/"
    web_2011_dir: "/data/apache/serve/echaloasuerte/legacy/app/web-2011/"
    web_2009_dir: "/data/apache/serve/echaloasuerte/legacy/app/web-2009/"
  tasks:

  - name: Install packages needed from apt
    apt:
      pkg: "{{ item }}"
      state: present
      update-cache: yes
    with_items:
    - git
    - gettext
    - apache2
    - libapache2-mod-php5
    - mariadb-server
    - python-mysqldb
    - php5-mysql

  - name: Remove apache default
    file:
        path: "/etc/apache2/sites-enabled/000-default.conf"
        state: absent

  - name: Create a database user (for the first run only)
    mysql_user: name=root password=toor priv=*.*:ALL host=localhost state=present
    ignore_errors: yes

  - name: Create a new database
    mysql_db: name=echaloasuerte state=present collation=utf8_general_ci login_user=root login_password=toor

  - name: Create tables
    shell: cat echloasuerte_legacy/create_db.sql | mysql -u root -ptoor echaloasuerte

  - name: Clone/update repository in {{ root_app_dir }}
    git:
      repo: git://github.com/etcaterva/Echaloasuerte_legacy.git
      dest: "{{ root_app_dir }}"
      update: yes
      accept_hostkey: True
      force: yes

  - name: Create echaloasuerte application tree
    file:
      path: "{{ item }}"
      state: directory
      owner: www-data
      recurse: yes
    with_items:
    - "{{ root_app_dir }}"
    - "{{ root_log_dir }}"
    - "{{ root_log_dir }}/2009"
    - "{{ root_log_dir }}/2011"

  - name: Fix owners of echaloasuerte application tree
    file:
      path: "{{ item }}"
      state: directory
      owner: www-data
      recurse: yes
    with_items:
    - "{{ root_app_dir }}"
    - "{{ root_log_dir }}"

  - name: Create the apache config file
    template:
      src: echaloasuerte_legacy/apache_website.conf
      dest: /etc/apache2/sites-available/echaloasuerte_legacy.conf
    notify:
    - restart apache

  - name: Enable apache site ecaheloasuerte
    file:
      src: ../sites-available/echaloasuerte_legacy.conf
      dest: /etc/apache2/sites-enabled/echaloasuerte_legacy.conf
      state: link
    notify:
    - restart apache

  - name: Ensure apache is running
    service:
      name: apache2
      state: started

  handlers:
    - name: restart apache
      service:
        name: apache2
        state: restarted
        sleep: 5
